# PARITY_MATRIX.md

Ledger of every source feature → Bedrock implementation status.
Component-level detail lives in `SOURCE_MAP.json` (912 rows, one per `SOURCE_INVENTORY.json` entry). This file tracks family-level rollups; both must end with no `uninspected`/`unknown`/`in_progress` rows at final release.

Reconciled 2026-09-15 from repository baseline `4bb5135fb508545227ffa110685e8aaca62ae36f`. The component ledger is checked against the deployed `BP/` and `RP/` trees by `tools/reconcile_parity_ledgers.py`; `ported` means a current artifact or scripted adapter exists, while `approximation`, `unsupported`, and `excluded` remain explicit outcomes.

Status values: `uninspected` → `analyzed` → `in_progress` → `ported` → `validated`, or terminal `blocked`/`excluded`.
Parity: `full` / `high` / `approximation` / `unsupported` / `unknown`.

| Source Feature | Source Evidence | Bedrock Implementation | Status | Parity | Validation | Notes |
|---|---|---|---|---|---|---|
| Mod bootstrap/lifecycle | TheBrokenScript.class, TBSEngineControl | `BP/scripts/main.js` + `BP/scripts/core/*` | ported | full | add-on validator; BP JavaScript syntax check | Deployed BP is the shipping bootstrap authority. |
| Shared runtime library (brokencore) | jarjar brokencore-0.1.0 | `BP/scripts/core/*` + shared story/state services | ported | high | parity ledger; add-on validator | Library behavior is absorbed; the Java jar is not shipped. |
| Registries (entities/blocks/items/etc.) | registry/ 545 classes | Current BP/RP JSON roster + script registries | ported | approximation | 912-row source-map reconciliation | 69 entities, 123 source blocks, 192 source items; aliases and engine gaps are explicit in `SOURCE_MAP.json`. |
| Entity-family fidelity contract (69 shipped entities) | `SOURCE_MAP.json` entity rows; `decompiled/net/thebrokenscript/entity/**` | `BP/scripts/core/entity_family_registry.js` + shared targeting/gaze/visibility services + family controllers | ported | high/approximation | `tests/entity_family_fidelity.test.mjs`; JavaScript syntax; GitHub Actions | Every BP entity has one source/runtime/render contract; supported dimension, hitbox-ray, spectator-filter, and sky-light behavior is validated, while Java-only multipart/render/chunk behavior stays explicit in A-023. |
| Circuit family (6 entities) | TBSEntities CIRCUIT* (6), entity/circuit/*, handlers/circuit/*, BIOME_BLACKLIST | BP/RP entity pairs + circuit controller/spawn rules | ported | high | add-on validator; entity/controller tests | Gaze, wall-climb, boat, and grace-overlay behavior remain documented adaptations. |
| Null watcher/scare (4 entities) | TBSEntities NULL_WATCHING/SCARE/MINING/IS_HERE, entity/nullent/* | BP/RP entity pairs + null controller/spawn rules | ported | high | add-on validator; entity/controller tests | Dimension teleport and client-FOV cases are adapted. |
| Null pursuit/endgame (7 entities incl. alias) | TBSEntities NULL_CHASE(nulll alias)/MAZE/ENDGAME/UNBEATABLE/FLYING/INVADE, entity/nullent/* | BP/RP entity pairs + null pursuit controller | ported | high | add-on validator; entity/controller tests | Maze doors and flying-FOV behavior are adapted. |
| Entities & variants (remaining) | entity/ 216 classes; TBSEntities (52 remaining) | Current BP/RP entity pairs + family controllers | ported | approximation | 912-row source-map reconciliation | `nothingiswatching` maps to `niw`; `fake_player` is blocked; `liberty` is the source's vanilla Allay helper and is excluded. |
| Stalking/psychological AI | circuit_stalk/stare, siluet_stare, tbe_stalk, fever_stalk, null_watching | Script finder/gaze/effects/refs/spawn framework | ported | high | add-on validator; controller tests | Gaze cone, per-entity FSM, and gate differences are explicit. |
| Bosses (Integrity P1–3, Jimmy, Kerfur) | boss/, entity/integrity/phase1..3, fractured/, Kerfur sources | Integrity, Phase 3, Fractured/Rock, Jimmy, and promotion runtimes | ported | approximation | Node regressions; add-on validators; GitHub Actions | Source timings, attack constants, lifecycle guards, and multipart logical model are retained; rendered bone contact and Java audio/render hooks remain adapters. |
| Fractured animation and rendered contact | `fractured.animation.json`, `FracturedModel`, `BaseFracturedEntity` controller mappings | `BP/scripts/systems/fractured_animation_model.js` + RP animation/geometry | ported | approximation | animation/model regression tests | Server damage origin uses the documented spatial adapter because render-bone transforms are not script-visible. |
| Fractured audio lifecycle | `BaseFracturedEntity.playSpawnSounds`, `JimArena`, `AudioFader` | Fractured runtime sound-handle lifecycle | ported | approximation | JavaScript syntax and regression checks | Source cue/reset ordering is retained; Java fade/attenuation is engine-specific. |
| Projectiles | chord_projectile, integ_fireball, fractured/Rock, integrity_ball damage type | Chord, Phase 3 fireball, Fractured/Rock, and logical multipart projectile adapters | ported | approximation | projectile/particle regressions; add-on validators | Source speed/damage/impact branches are modeled; bone-muzzle and continuous sweep remain adapters. |
| Damage types (15) | data/thebrokenscript/damage_type/*.json + TBSDamageTypes | `damage_source_model.js` + native attribution runtime | ported | approximation | damage regression suite; JavaScript syntax check | Native cause/entity/projectile attribution is used; custom registration and exact bypass/death-message behavior are unsupported. |
| Status effects (2) | TBSEffects | Scripted effect/state emulation | ported | approximation | source-map and add-on validators | No custom potion registry is claimed. |
| Player/world/persistent state | TBSDataAttachments, handlers | `BP/scripts/systems/{player_state,world_state}.js` + core state | ported | high | state regression tests; add-on validator | Source attachment defaults and persistence gates are represented. |
| Events engine (86 registered) | events/, TBSEvents lang keys | Source-backed H registry + event frequency + story thresholds | ported | approximation | source-backed event audit; source-map reconciliation | 86 source-backed H handlers are registered and selected by the source-aware event engine; story/source-only rows remain explicit in the ledger. |
| Chat responses (42 registered responses) | TBSChatResponses, responses/ | `BP/scripts/systems/horror_chat.js` + chat audit | ported | approximation | chat audit; add-on validator | The runtime carries all 42 ordered source response contracts; aliases, delivery scope, delays, and engine-only adaptations are checked by the chat model/audit tests. |
| Story progression (stages) | TBSStoryEvents, events/story | `story_time.js` + `story_clock_model.js` + story events + signed null-book adapter | ported | high | `tests/story_clock.test.mjs`; story regressions; add-on validators | Java daylight/player gate, persisted-time no-player dispatch, exact thresholds days(5,6,10,12,15,20,24,32,38,48)+1000, and persistent delivery gates are preserved. |
| Blocks (123) + block entities (8) | blockstates/, block/, TBSBlockEntities | 123 BP block JSON files + scripted components/loot | ported | approximation | 912-row reconciliation; add-on/resource validators | Component/state behavior is explicit; Java block-entity classes are not represented as one-to-one JSON. |
| Items (192) | item lang ids, TBSItems/TBSEasterEggItems/TBSPlushies | 76 direct BP items + 123 implicit block-item forms + scripted items | ported | approximation | 912-row reconciliation; add-on validators | Functional item hooks and Java inventory/render differences remain documented. |
| Fluid void_liquid | neoforge/fluids, TBSFluids | Void goop still/flow blocks + bucket + scripted effects | ported | approximation | resource/add-on validators | Bedrock custom-fluid physics are unavailable. |
| Dimensions (13) | data/dimension/*.json | Startup custom-dimension registration + safe landing generator | ported | approximation | script/type checks; add-on validator | All 13 logical IDs are addressable; Java noise/generator semantics are adapted. |
| Portals | block/portal/, portal_controller/extender items | Portal blocks/items + custom block/feature components | ported | approximation | add-on/resource validators | Safe arrival and cross-dimension behavior use the current script adapter. |
| Biomes (15) + carver/features/noise | worldgen/** | 15 BP biomes + RP clients + scripted worldgen | ported | approximation | add-on/worldgen validators | Java noise and decorator placement are documented approximations. |
| Structures (source corpus + shaft jigsaw + xcsf) | structure/, xcsf_structure/ | Shaft NBT/pools + scripted arena/worldgen models | ported | approximation | Jigsaw/NBT/structure validators | Unused corpus and Java XCSF placement are explicitly excluded or adapted. |
| Shaft Jigsaw | `data/thebrokenscript/structure/shaft/*.nbt` (corner, hall, junction, room, room_hall, root) | Six current `BP/structures/.../*.nbt` templates + three pools + structure JSON | ported | approximation | NBT connector graph; Jigsaw validator | Template contents are source-backed; natural placement/frequency is not claimed exact. |
| Spawn conditions/modifiers (33) | biome_modifier/spawns, TBSSpawnConditions | Script spawn director + entity-family spawn rules | ported | approximation | add-on validator; source-map reconciliation | Java placement weights/scheduler behavior are adapted; inactive source markers are excluded. |
| Recipes (40) | data/recipe/*.json | 40 matching BP recipe JSON files | ported | full | add-on/resource validators | Every source recipe resolves to a current BP recipe file. |
| Loot tables | loot_table/blocks(150+)/entities(1) | Generated BP block loot + `null_cod` entity loot | ported | approximation | add-on validator | Aggregate source rows resolve to shipped loot; provider identity is not overstated. |
| Tags | data/*/tags/** | Script predicates/component checks | ported | approximation | add-on validator | No unsupported BP tag directory is claimed. |
| Advancements (5) | advancement/*.json + triggers | Progression flags + toast/title-equivalent UX | ported | approximation | progression regressions; add-on validator | Bedrock custom advancement registration is unavailable. |
| Commands + fx toggles | command/, command/dev | Script command/progression surface | ported | approximation | JavaScript syntax; add-on validator | Java registration and permission hooks are adapted. |
| Networking (music/debug/gui sync) | network/, TBSPackets | Server-authoritative script flow | blocked | unsupported | source-map reconciliation | Java packet interception and client synchronization hooks are unavailable. |
| Animations/render/audio/camera/UI | client/, assets | Current RP geometry/animation/audio/UI + BP presentation adapters | ported | approximation | resource links; JavaScript syntax; add-on validator | Resource assets are present, but camera, render hooks, UI internals, and audio lifecycle remain adapters. |
| Particles (9 defs) | assets/thebrokenscript/particles/*.json + registered client particle providers | Nine RP emitters + BP particle model/runtime | ported | approximation | particle regressions; add-on validators | Provider/render-hook differences are explicit. |
| Java GLSL shaders (89 files) | shaders/** | VHS/overlay approximation layer | blocked | unsupported | resource/add-on validators | Bedrock has no equivalent Java GLSL pipeline. |
| GUI screens/menus (10 menus + overlays) | TBSMenus, client/gui | Bedrock forms, titles, actionbars, and UI definitions | ported | approximation | add-on validator | Java screen internals and desktop effects are not claimed. |
| Desktop integration (window title/LWJGL/JFrame/txt files/ban-kick) | window.* keys, events/jframe, config enableFileCreation | In-game title/actionbar substitutes only | blocked | unsupported | source-map reconciliation | OS window, file, and process hooks are outside the Bedrock sandbox. |
| Config surface (gameplay-affecting) | config/common + lang configuration keys | BP config/default layer | ported | approximation | add-on validator | Gameplay defaults are present; client-only NeoForge config plumbing is adapted. |
| Music discs (12 jukebox songs) | jukebox_song/, TBSSongs | Current disc/items + RP sounds + script playback | ported | approximation | resource/add-on validators | Java jukebox/network synchronization is adapted. |
| Paintings (1) | painting_variant/circuit_cave | Circuit-cave item/entity surrogate | ported | approximation | resource/add-on validators | Bedrock has no custom painting-variant registration path. |
| Embedded nostalgia packs | nostalgia/, nostalgia_gen/ | Documented optional/out-of-pack decision | excluded | unsupported | source-map reconciliation | Not required for the current runtime add-on. |
| books/book0.book, sites/rblog/file.bin | misc binaries | Signed-book model/adapter; opaque external payload excluded | excluded | unsupported | story-book regressions; source-map reconciliation | The book is parsed into runtime content; `file.bin` has no supported Bedrock representation. |

Chunk-level completion updates this table; SOURCE_MAP.json carries the 912-row component detail.
