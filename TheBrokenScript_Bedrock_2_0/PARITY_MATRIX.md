# PARITY_MATRIX.md

Ledger of every source feature → Bedrock implementation status.
Component-level detail lives in `SOURCE_MAP.json` (912 rows, one per `SOURCE_INVENTORY.json` entry). This file tracks family-level rollups; both must end with no `uninspected`/`unknown`/`in_progress` rows at final release.

Status values: `uninspected` → `analyzed` → `in_progress` → `ported` → `validated`, or `blocked`.
Parity: `full` / `high` / `approximation` / `unsupported` / `unknown`.


## Chunk 54 update

The Stage 2 boss adapter now has a portable runtime scaffold: stage2GeneratorRuntimeVolumes preserves the source 10×10 chunk cell and floor/barrier Y layers, while ensureStage2RuntimeScaffold applies air-only Dimension.fillBlocks operations once per cell before floor entities spawn. This improves the supported Phase 2 floor lifecycle in the void dimension without changing the explicit status of Java room/template/occupancy generation.


## Chunk 55 update

The Stage 2 template boundary now has one validated Bedrock asset: the source-audited `stone1` Floor 4 default (16×1×16, 256 `minecraft:stone` blocks, no entities or block entities) is stored at `BP/structures/thebrokenscript/stage2/stone1.mcstructure`. The pure placement plan preserves the source blob and Y 233 placement band, and the runtime exposes an opt-in `Dimension.runCommand` structure-load seam for `none` mirror only. The remaining 63 templates, Java FRONT_BACK mirror mapping, automatic scheduler placement, occupancy, border/tunnel/nowhere generation, and live Bedrock smoke testing remain open.
## Chunk 56 update

The Stage 2 asset catalog now validates the first rare Floor 4 variant, `stone2`: 16×9×16, 2304 cells, 256 `minecraft:stone` + 50 `thebrokenscript:block_is_missing_id` + 1998 `minecraft:air`, no entities or block entities. Its source metadata, no-mirror placement plan, and structure-load command are source-backed; validated count is 2/64. The other 62 templates, FRONT_BACK mirror mapping, automatic scheduler placement, occupancy, border/tunnel/nowhere generation, and live Bedrock smoke testing remain deferred.

## Chunk 57 update

The Stage 2 asset catalog now validates the second rare Floor 4 variant, `stone3`: 16×4×16, 1024 cells, 256 `minecraft:stone`, 724 `minecraft:air`, and 44 `minecraft:oak_door` cells represented by 14 palette states. The Bedrock asset preserves the source door orientation, hinge, open, and upper/lower state fields with the current `minecraft:cardinal_direction`, `door_hinge_bit`, `open_bit`, and `upper_block_bit` names; Java `powered=false` is omitted because Bedrock oak doors have no powered state. Validated count is 3/64. The remaining 61 templates, automatic scheduler placement, Java custom-generator RNG/occupancy, FRONT_BACK mirror mapping, borders/tunnels/nowhere generation, and live Bedrock smoke testing remain deferred. The Floor 1–7 boundaries and Floor 6 → `FLOOR_6_INTEG` mapping remain preserved.
## Chunk 58 update

The Stage 2 asset catalog now validates the third rare Floor 4 variant, `stone4`: 16×3×16, 768 cells, eight palette states, 295 non-air primary cells, and one minecart entity. The Bedrock structure retains typed rail states and an entity-bearing placement plan with `includeEntities=true`; the pure Floor 4 variant mapping, Floor 1–7 boundaries, and Floor 6 → `FLOOR_6_INTEG` mapping are unchanged. The initial implementation gate caught malformed NBT zero-byte terminators and the corrected asset passes the repository structure validator. Validated count is 4/64; the remaining 60 templates, automatic scheduler placement, Java custom-generator RNG/occupancy, FRONT_BACK mirror mapping, borders/tunnels/nowhere generation, and live Bedrock smoke testing remain deferred.

## Chunk 59 update

The Stage 2 asset catalog now validates the fourth rare Floor 4 variant, `stone5`: 16×3×16, 768 cells, 256 `minecraft:stone`, 489 `minecraft:air`, and 23 source `thebrokenscript:it` cells. The Bedrock asset maps the unavailable source custom block to the existing `thebrokenscript:block_is_missing_id` surrogate, retains 512 non-air primary cells, and has no entities or block entities. The Floor 4 variant mapping, Floor 1–7 boundaries, and Floor 6 → `FLOOR_6_INTEG` mapping are unchanged. Validated count is 5/64; the remaining 59 templates, automatic scheduler placement, Java custom-generator RNG/occupancy, FRONT_BACK mirror mapping, borders/tunnels/nowhere generation, and live Bedrock smoke testing remain deferred.

## Chunk 60 update

The Stage 2 asset catalog now validates the fifth rare Floor 4 variant, `stone6`: 16×4×16, 1024 cells, 256 non-air primary cells, and 15 Java armor stands. The Bedrock asset preserves the armor-stand entity count and relative positions/rotations through `minecraft:armor_stand` structure entities and uses `includeEntities=true`; the Java player-head profile/skin payload is explicitly omitted because no verified portable Bedrock structure representation exists for that source profile. The Floor 4 variant mapping, Floor 1–7 boundaries, and Floor 6 → `FLOOR_6_INTEG` mapping are unchanged. Validated count is 6/64; the remaining 58 templates, automatic scheduler placement, Java custom-generator RNG/occupancy, FRONT_BACK mirror mapping, borders/tunnels/nowhere generation, and live Bedrock smoke testing remain deferred.


## Chunk 61 update

The Stage 2 asset catalog now validates the sixth rare Floor 4 variant, stone7: 16×3×16, 768 cells, 256 minecraft:stone, 32 minecraft:smooth_stone, 480 minecraft:air, and no entities or block entities. The Bedrock structure preserves the source block layout in a format-version 1 little-endian .mcstructure, and the placement plan emits includeEntities=false at Y 233. The Floor 1–7 boundaries, Floor 6 → FLOOR_6_INTEG mapping, and Java Stage2Generator custom-generation boundary are unchanged. Validated count is 7/64; 57 source templates, automatic scheduler placement, exact Java RNG/occupancy/rotation/mirror, and live Bedrock smoke testing remain deferred.
| Source Feature | Source Evidence | Bedrock Implementation | Status | Parity | Validation | Notes |
|---|---|---|---|---|---|---|
| Mod bootstrap/lifecycle | TheBrokenScript.class, TBSEngineControl | Chunk 01 bootstrap.ts → src/main.js + core modules | ported (foundation) | full (infrastructure) | validate_pack.ps1 PASS; runtime test pending Minecraft install | |
| Shared runtime library (brokencore) | jarjar brokencore-0.1.0 | decompiled (815 files); StoryEvent/clock/config semantics absorbed into src/shared+core | ported | high | validator PASS | |
| Registries (entities/blocks/items/etc.) | registry/ 545 classes | JSON definitions + ID constants module | uninspected | unknown | — | roster enumerated: 69 entities, 123 blocks, 192 items |
| Circuit family (6 entities) | TBSEntities CIRCUIT* (6), entity/circuit/*, handlers/circuit/*, BIOME_BLACKLIST | BP/RP entities + controller + spawn rule (stalk predicate ported: skyLight, blacklist, 420, 5200) | ported | high | validate_pack PASS; runtime chase fidelity approximated | wall-climb destroy, boat discard, grace overlay skipped for high parity |
| Null watcher/scare (4 entities) | TBSEntities NULL_WATCHING/SCARE/MINING/IS_HERE, entity/nullent/* | BP/RP entities + controller (8000/40/1200/500 timers, 10-case anger, mining build, flying approach) + NullConditions port (7200 delay, sky visible, isNullHere) | ported | high | validate_pack PASS | dimension teleport cases approximated |
| Null pursuit/endgame (7 entities incl. alias) | TBSEntities NULL_CHASE(nulll alias)/MAZE/ENDGAME/UNBEATABLE/FLYING/INVADE, entity/nullent/* | BP/RP entities + pursuit controller (450/3200/420/500 timers, chase/maze light, endgame 999 pulse, unbeatable invuln) | ported | high | validate_pack PASS | maze door break, flying FOV sneak approximated |
| Entities & variants (remaining) | entity/ 216 classes; TBSEntities (52 remaining) | Chunks 05D–05F per family | uninspected | unknown | — | |
| Stalking/psychological AI | circuit_stalk/stare, siluet_stare, tbe_stalk, fever_stalk, null_watching… | framework live (finder/gaze/effects/refs/spawn director/delay timers); per-entity FSMs in 05A–05F | ported (framework) | high | validate_pack PASS | gaze=cone approximation; gate omissions ledgered per rule |
| Bosses (Integrity P1–3, Jimmy, Kerfur) | boss/, entity/integrity/phase1..3, fractured/, Kerfur sources | Integrity Arena/Phase source model + CorruptedCommandBlockConfirmPacket → Arena.Companion.start → Phase1.start handoff (Chunks 49–50), including source center/roster/timing/intro/Chord gating, Phase 1 terrain queue/tick replacement, Phase 2 20-tick Stage2 roster transfer, and Phase 2 recovery tick scheduling/routing, Stage 2 floor/entity spawning, safe-block scanning, tether floor queries, and tether-gated Integrity placement; Phase 3 ring/boundary/GroundAttack/GroundArm/multi-attack/damage-death runtime; dedicated Fractured/Rock attack runtime (Chunks 28, 33, 35, and 37); source-backed Jimmy multipart model/runtime, FracturedRoam host lifecycle, JimArena schedule, and promotion adapter (Chunks 29–31); fabricated P1/P2 health-threshold transitions removed | in_progress | approximation | Chunk 53 focused Stage 2 regressions plus preceding Integrity/Jimmy/Phase3 regressions; GitHub Actions [run 33906338464](https://github.com/PastaHimself/tbs-2.0/actions/runs/33906338464) passed validators, beta type-check, JavaScript regressions, Blockception diagnostics, Creator Tools validation, and packaging; artifact uploads logged the known quota message but were non-blocking | Phase 1 now corrupts one queued topmost block every 20 ticks using the four recovered replacement IDs and skips the corrupted command block. Once the tracked Chords are all dead, the supported adapter tears down Phase 1 and transfers the participant roster to the existing Stage2 dimension after the recovered 20-tick delay; the active Phase 2 tick now applies the recovered Y 190–198 recovery teleport, tracks the lowest participant's mapped floor, spawns each entered Stage 2 floor once, and places Integrity only after target-floor Tether gates pass. Phase 3 preserves ring, boundary, attacks, caps, hurt gate, mace parry, and 298-tick cleanup. Jimmy preserves 100-tick delay, selector weights, attack lengths, source-named attack clips and keyframe ticks, direct Bedrock animation playback, stomp/slam/rock/air-lift constants, Rock owner exclusion, the 400-particle moon-stone block burst adapter, six logical multipart definitions with role-specific yaw transforms, arrow side-effect order, the 149-tick Roam rising guard, full Roam lifecycle adapters, and the 103-tick promotion duration. Source Jimmy spawn cue and SoundInstance-backed JimArena intro/loop cleanup are now included. Chunk 41 adds a source-backed contact resolver with injectable rendered bone positions and explicit source/anchor fallbacks; a live rendered-bone bridge remains engine-limited. |
| Fractured animation and rendered contact | `fractured.animation.json`, `FracturedModel`, `BaseFracturedEntity` controller mappings | `fractured_animation_model.js` source timeline/presentation model + `fractured_contact_model.js` source-backed spatial resolver + `fractured_runtime.js` direct `playAnimation` bridge; geometry locators and animation timeline particle events for contact presentation | ported (adapter) | approximation | Chunk 41: 4 focused contact regressions plus prior animation regressions; changed resolver/runtime JS syntax and beta type-check PASS; touched animation/entity/geometry/particle JSON parse and locator/timeline assertions PASS | Attack clip names, source seconds, 20 Hz event ticks, Roam Spawn/Idle/Walk/Flee/Underground/Loss mappings, and visual contact locators are preserved. Server-side damage/contact origin now routes through the resolver: exact rendered positions are accepted when an external bridge supplies them, SingleStomp falls back to the recovered source offset, and client-only claw/rock contacts fall back to the entity anchor because GeckoLib render-bone world transforms are not script-visible. |
| Fractured audio lifecycle | `BaseFracturedEntity.playSpawnSounds`, `JimArena`, `AudioFader` | `fractured_runtime.js` spawn cue + SoundInstance handle retention/stop | ported (adapter) | approximation | 5/5 focused Fractured runtime regressions; changed runtime `node --check` PASS; GitHub Actions run 125 PASS | Source cue and reset cleanup preserved; Java FancyAudio/fade/attenuation remain engine-specific. |
| Projectiles | chord_projectile, integ_fireball, fractured/Rock, integrity_ball damage type | Chord source-specific runtime correction (Chunks 26 and 32) + Phase 3 fireball runtime + dedicated Fractured/Rock runtime (Chunks 28–29, 33); conceptual multipart arrow routing | ported (slices) | approximation | Chunk 33 focused projectile/particle regressions plus preceding Jimmy/Phase3 checks; GitHub Actions validation gates pass on the published commit, with artifact uploads blocked by repository storage quota. | Chord uses source speed/distance/impact branches, inherited vanilla difficulty-weighted arrow damage, and gravity restoration; Rock preserves source 15 damage, speed 8, impact AOE, owner exclusion, AirLift pulse, and the 400-particle moon-stone block-impact burst adapter. Jimmy arrow hits require a conceptual logical part, distinguish parent parts from leg subentities, and preserve burning/spectral side-effect timing through a deferred adapter. Bone-muzzle and continuous projectile sweep remain documented adapters. |
| Damage types (15) | data/thebrokenscript/damage_type/*.json + TBSDamageTypes | damage_source_model.js catalog + damage_source_runtime.js native-cause/entity/projectile attribution adapter | ported (adapter) | approximation | 53/53 Node regressions; changed runtime/model JavaScript node --check PASS | Source ids, messages, effects, exhaustion, scaling, no-knockback, and registry bypass flags are catalogued exactly; Bedrock custom registration and exact bypass/death-message behavior remain engine gaps |
| Status effects (2) | TBSEffects, HeartCorruptionMobEffect, WhyCantYouLeaveMobEffect | status_effect_model.js source catalog + status_effect_runtime.js health-cap adapter; dynamic expiry and Eyes-particle bridge in ported_features.js | ported (adapter) | approximation | Chunk 39: 6 focused status-effect regressions; changed runtime node --check PASS; legacy effect regression updated; runtime smoke pending | Bedrock cannot register custom effect ids or install Java attribute modifiers; active Heart Corruption is emulated by capping current health at effective maximum minus one, while Why Can't You Leave retains its source duration/particle contract. |
| Player/world/persistent state | TBSDataAttachments, handlers | src/systems/{world_state,player_state}.js + state service — full schema defaults ported | ported | high | validator PASS | MapVariables/PlayerVariables decompiled |
| Events engine (86 registered source IDs) | events/, TBSEvents.java, TBSEngineControl.java, StatisticsEventPicker.java, EventWeightTracker.java | horror_events.js + event_scheduler_model.js + event_frequency.js + config_defaults.js; Chunk 43 wires the source frequency curve, per-tick player selection, disabled IDs, persistent inverse occurrence weights, and configured rerolls; Chunks 45–47 wire NullInterface, NulledGui, and FakeDisconnect form adapters; Chunk 48 routes the command-block screen entry points through the block component seam | ported (adapter) | approximation | Chunk 43 scheduler contract plus Chunks 45–48 focused UI/event adapters; GitHub Actions run 33866668553 passed all validation and package gates, with artifact uploads blocked by repository storage quota | Source RandomEvent frequency, weighted selection, occurrence tracking, reroll/config contracts, active Null interface forms, NulledGui form/messages, FakeDisconnect heading/body/action, and command-block screen entry routing are mirrored. Java fake-midnight, panorama/native disconnect, and other custom GUI/window/NBT structure mechanisms retain explicit Bedrock fallbacks. |
| Chat responses (45) | TBSChatResponses, responses/ | chat keyword engine (Chunk 02/12) | uninspected | unknown | — | moved: chat engine lands with events chunk |
| Story progression (stages) | TBSStoryEvents, events/story | story_time clock + txt/coords/moon events plus signed null_book_hint written-book adapter with bounded delivery retry | ported (adapter) | high | Chunk 38: 7/7 focused story regressions, 67/67 full Node suite; GitHub Actions [run 129](https://github.com/PastaHimself/tbs-2.0/actions/runs/33718950818) PASS; runtime smoke pending | thresholds days(5,6,12,24,32,38,48)+1000 preserved; null_book_hint pages preserve source text, Java chunk-centered binary X/Z including INT_MAX, Y=201, null/null signing, independent online-player delivery, overflow drop, and persistence gate |
| Blocks (123) + block entities (8) | blockstates/, block/, TBSBlockEntities | Chunk 08 foundation + Chunk 48 command and command_block_giver custom-component adapters | in_progress | approximation | Chunks 48–49 command input/confirmation routing, overworld/initiator validation branches, giver give/remove behavior, source-backed Arena-start handoff, and focused wiring regressions pass; GitHub Actions run 33866668553 passed validation and packaging gates, with artifact uploads blocked by repository storage quota | Corrupted Command Block now selects the input or confirmation form from persisted codeApplied state; remaining block families and Java block-entity presentation remain ledgered. |
| Items (192) | item lang ids, TBSItems/TBSEasterEggItems/TBSPlushies | Chunk 09 base items + Chunk 44 Library Book reader + Chunk 48 Torn Paper item-use adapter | ported (partial) | approximation | Chunk 48 preserves Torn Paper title, source coordinate body, close action, item-component wiring, and focused regressions; GitHub Actions run 33866668553 passed validation and packaging gates, with artifact uploads blocked by repository storage quota | The Torn Paper item now opens a supported reader with source chunk-centered X/Z coordinates and Y 216; Java swing/texture presentation remains adapted. The Book item and remaining item families retain their prior status. |
| Fluid void_liquid | neoforge/fluids, TBSFluids | approximated translucent/damage block | uninspected | approximation-pending | — | Bedrock lacks custom fluids |
| Dimensions (13) | data/dimension/*.json | Chunk 10 (strategy pending) | uninspected | unknown | — | see BEDROCK_COMPATIBILITY decision |
| Portals | block/portal/, portal_controller/extender items | Chunk 08+10 | uninspected | unknown | — | safe arrival validation required |
| Biomes (15) + carver/features/noise | worldgen/** | Chunk 11 | uninspected | unknown | — | noise_settings = documented approximation |
| Structures (source corpus + shaft jigsaw + xcsf) | structure/, xcsf_structure/ | Six-piece Shaft now has source-backed NBT + native Jigsaw pools/structure; 64-template Integrity Stage 2 NBT audit/model; Chunks 55–61 validate seven Floor 4 stone assets and placement plans; remaining structure corpus and XCSF reconstruction still pending | in_progress | unknown | six Shaft source/BP Git blob SHAs match; JSON + binary-NBT connector validators present; Integrity model regressions + Stage2 metadata audit + validated Bedrock assets | Stage2 templates are source-present, with 7/64 now validated as Bedrock assets; Java custom-generator placement, remaining 57 conversions, and exact world placement are still blocked in Bedrock; do not treat native Shaft assembly, Arena source model, or Stage2 audits as full corpus/world-placement/XCSF parity |
| Integrity Stage 2 template placement | Stage2Generator.genRoom/placeStructure; STAGE2_GENERATOR_AUDIT.json | stage2TemplatePlacementPlan + stone1/stone2/stone3/stone4/stone5/stone6.mcstructure + stone7.mcstructure + opt-in runStage2TemplateLoad | in_progress | approximation | source metadata, focused TDD, validate_mcstructures, and TDD red run 34013092095 observed; implementation CI pending | 7/64 assets validated; stone6 preserves 15 armor-stand entities with source positions/rotations but omits Java player-head profile data; stone7 is a 16×3×16 block-only asset with 256 stone and 32 smooth_stone cells; Java custom Stage2Generator placement, remaining 57 conversions, entity/block-entity palettes, FRONT_BACK mirroring, occupancy, automatic scheduler placement, and exact world placement remain blocked |
| Shaft Jigsaw | `data/thebrokenscript/structure/shaft/*.nbt` (corner, hall, junction, room, room_hall, root) | `BP/structures/thebrokenscript/shaft/*.nbt` + `BP/worldgen/template_pools/{shaft_root,hallway,shaft_room}.json` + `BP/worldgen/structures/shaft.json` | in_progress | unknown | source-identical templates; NBT connector graph checks pool/target/name compatibility; regression tests added | Natural `structure_set` frequency/spacing and source pool weights remain pending authoritative Java placement evidence; current generation parameters are not promoted to full parity |
| Spawn conditions/modifiers (33) | biome_modifier/spawns, TBSSpawnConditions | spawn director (Chunk 02) + native rules | uninspected | unknown | — | |
| Recipes (40) | data/recipe/*.json | BP recipes Chunk 13 | uninspected | unknown | — | |
| Loot tables | loot_table/blocks(150+)/entities(1) | BP loot Chunk 13 | uninspected | unknown | — | |
| Tags | data/*/tags/** | BP/RP tags Chunk 13 | uninspected | unknown | — | |
| Advancements (5) | advancement/*.json + triggers | Bedrock-equivalent tracked state + toast-equivalent UX Chunk 13 | uninspected | approximation-pending | — | |
| Commands + fx toggles | command/, command/dev | script commands Chunk 13 | uninspected | unknown | — | |
| Networking (music/debug/gui sync) | network/, TBSPackets | server-authoritative script flow; Chunk 48 command-code validation and confirmation state adapter | uninspected | unknown | Chunk 48 source packet decision branches are covered by focused regressions; GitHub Actions run 33866668553 passed validation and packaging gates, with artifact uploads blocked by repository storage quota | Remaining music/debug/gui synchronization packets and native packet transport are still uninspected. |
| Animations/render/audio/camera/UI | client/, assets | RP foundation built: geo×77 (26 ids fixed), anims×36, sounds+defs×143, textures×502, atlases, flipbooks×5, lang | ported (foundation) | full (asset layer) | validate_pack PASS; per-entity controllers land in Chunks 04–05x/14 |
| Particles (9 defs) | assets/thebrokenscript/particles/*.json + registered client particle providers | RP particle emitters for all nine identifiers + particle_model.js/particle_runtime.js; Null/Eyes/Curved callsite bridge | ported (adapter) | approximation | Chunk 36: 8 focused particle regressions plus 59 existing regressions PASS; all touched particle JSON parsed locally; GitHub Actions run 123 PASS | Provider sizes/lifetimes/materials and source event counts/spread are preserved. Paper crossed-quads and resource-only definitions remain explicit adapters. |
| Java GLSL shaders (89 files) | shaders/** | approximation layer; pipeline unsupported | uninspected | unsupported-pending | — | VHS/aberration/invert/dream/fever/glitch/sky |
| GUI screens/menus (10 menus + overlays) | TBSMenus, client/gui | forms + overlays; Chunk 44 LibraryBookScreen + Chunk 45 NullInterface + Chunk 46 NulledGui + Chunk 47 FakeDisconnect + Chunk 48 TornPaper and CommandBlockGui/Confirm adapters in ported_features.js | ported (partial) | approximation | Chunk 48 preserves source coordinates, visible strings/actions, validation order, item/block wiring, and focused regressions; GitHub Actions run 33866668553 passed validation and packaging gates, with artifact uploads blocked by repository storage quota | LibraryBookScreen, NullInterface, NulledGui, FakeDisconnect, TornPaper, and CommandBlockGui/Confirm are represented by supported forms. Java textures, 15° rotation, panorama, forced-close/native disconnect transition, EditBox glitch animation, packet/window-title transport, native Integrity Arena packet/camera presentation, and other custom presentation remain deferred. |
| Desktop integration (window title/LWJGL/JFrame/txt files/ban-kick) | window.* keys, events/jframe, config enableFileCreation | ENGINE_UNSUPPORTED except in-game kick/title equivalents | uninspected | unsupported-pending | — | documented in KNOWN_LIMITATIONS |
| Config surface (gameplay-affecting) | config/common + lang configuration keys | config layer seeded with decompiled defaults (world/danger/entity groups, 15 values) | ported (partial groups) | full | validator PASS | remaining client/accessibility/menu groups land in Chunk 14 |
| Music discs (12 jukebox songs) | jukebox_song/*.json, TBSSongs, TBSSounds | music_disc_model.js catalog + native minecraft:record components on all 12 existing Bedrock music items; existing RP sound definitions/assets reused | ported (adapter) | approximation | Chunk 40: 2 focused model regressions plus 12 item/sound-link assertions; runtime smoke pending | Source durations, custom sound-definition keys, descriptions, item identity, and non-stackable behavior are preserved. Java comparator output 15 is clamped to Bedrock's documented maximum 13. |
| Paintings (1) | painting_variant/circuit_cave | approximation (no custom painting variants) | uninspected | approximation-pending | — | |
| Embedded nostalgia packs | nostalgia/, nostalgia_gen/ | optional RP or documented exclusion | uninspected | unknown | — | decision Chunk 03 |
| books/book0.book, sites/rblog/file.bin | misc binaries | parse book format Chunk 12; bin external-only | uninspected | unknown | — | |

Chunk-level completion updates this table; SOURCE_MAP.json carries the 912-row component detail.
